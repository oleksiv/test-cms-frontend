import { TreeViewDirective } from './tree-view.directive';

describe('TreeViewDirective', () => {
  it('should create an instance', () => {
    const directive = new TreeViewDirective();
    expect(directive).toBeTruthy();
  });
});
